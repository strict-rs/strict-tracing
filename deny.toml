# cargo-deny configuration.
#
# See https://embarkstudios.github.io/cargo-deny/ for the full schema. This
# template configures strict-but-reasonable defaults:
#   - workspace crates have `publish = false` and carry the repo's ARR LICENSE,
#     which is not SPDX-expressible; ignore the license check for them.
#   - allow the permissive licenses commonly found on crates.io; reject anything
#     else so copyleft dependencies surface before they ship.
#   - ban non-crates.io sources and duplicate versions.

# Audit the no-defaults + all-features graph so feature-gated git sources
# (proptest / snapbox / trybuild, through strict-test-support's `full` feature)
# listed in `[sources].allow-git` are genuinely exercised instead of reported
# as unmatched.
[graph]
all-features        = true
no-default-features = true

[licenses]
confidence-threshold = 0.9
private = { ignore = true }
allow = [
  "MIT",
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Unicode-3.0",
  "Unicode-DFS-2016",
  "Zlib",
  "CC0-1.0",
  "MPL-2.0",
]

[bans]
multiple-versions          = "warn"
wildcards                  = "deny"
highlight                  = "all"
workspace-default-features = "allow"
external-default-features  = "allow"
# Skip only the legacy syn major so a genuinely new duplicate still warns:
# the workspace's macro crates build on syn 3, while the external proc-macro
# ecosystem (argh_derive, darling, the ratatui stack) still tracks syn 2 and
# reaches the graph solely through tracing-examples dev-dependencies.
skip = [
  { crate = "syn@2", reason = "external proc-macro dependencies of tracing-examples still track syn 2 alongside the workspace's syn 3" },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = [
  "https://github.com/rust-lang/crates.io-index",
]
allow-git = [
  "https://github.com/earthlings-dev/agents-md-rs.git",
  "https://github.com/earthlings-dev/rust-mutant-fleet.git",
  "https://github.com/earthlings-dev/strict-test-support-rs.git",
  "https://github.com/earthlings-dev/template-rs.git",
  "https://github.com/earthlings-dev/strict-standard-rs.git",
  "https://github.com/strict-rs/strict-bpaf.git",
  "https://github.com/strict-rs/strict-rowan.git",
  "https://github.com/strict-rs/strict-taplo.git",
  "https://github.com/strict-rs/strict-thiserror.git",
  "https://github.com/strict-rs/strict-trybuild.git",
]

[advisories]
yanked = "deny"
ignore = []
