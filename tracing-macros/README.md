# tracing-macros

`tracing-macros` provides experimental macros for emitting trace events through
the `tracing` instrumentation ecosystem.

This crate is maintained as part of the `strict-tracing` workspace.
